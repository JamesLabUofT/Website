```{=html}
<ul class="list">
<% for (const item of items) { %>
  <li <%= metadataAttrs(item) %>>
    <% if(item.link){ %>
    <a href="<%- item.link %>" class="listing-name"><%= item.description %></a><br/>
    <% } else{ %>  
    <span class="listing-name"><%= item.description %></span><br/>
    <% } %>
    
    <span class="listing-custom-field"><%= item.date %><span>
    </li>
<% } %>
</ul>
```